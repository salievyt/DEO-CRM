from django.db import models

# Cabinet module uses existing models from other apps.
# No additional models needed — it's a view-only module
# that provides a client-facing interface to projects,
# documents, invoices, and messages.
